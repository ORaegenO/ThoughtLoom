<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= note.title %></title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🧵</text></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <small class="text-muted">
                    <a href="/projects/<%= note.category.project._id %>"><%= note.category.project.name %></a> → 
                    <a href="/categories/<%= note.category._id %>"><%= note.category.name %></a>
                </small>
                <h1><%= note.title %></h1>
            </div>
            <a href="/categories/<%= note.category._id %>" class="btn btn-outline-secondary">Back to Category</a>
        </div>
        
        <div class="card">
            <div class="card-body">
                <p class="card-text" style="white-space: pre-wrap;"><%= note.content || 'No content' %></p>
            </div>
        </div>
        
        <div class="mt-3">
            <a href="/notes/<%= note._id %>/edit" class="btn btn-warning">Edit Note</a>
            <form action="/notes/<%= note._id %>?_method=DELETE" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Delete this note?')">Delete Note</button>
            </form>
        </div>
    </div>
</body>
</html>