<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Edit Note</h1>
        <p class="text-muted">Category: <strong><%= note.category.name %></strong></p>
        
        <form action="/notes/<%= note._id %>?_method=PUT" method="POST" class="mt-4">
            <div class="mb-3">
                <label for="title" class="form-label">Note Title *</label>
                <input type="text" class="form-control" id="title" name="title" value="<%= note.title %>" required>
            </div>
            
            <div class="mb-3">
                <label for="content" class="form-label">Content</label>
                <textarea class="form-control" id="content" name="content" rows="8"><%= note.content %></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Update Note</button>
            <a href="/notes/<%= note._id %>" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>