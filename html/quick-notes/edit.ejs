<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Quick Note - ThoughtLoom</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>✏️ Edit Quick Note</h1>
            <a href="/quick-notes" class="btn btn-secondary">← Back to Quick Notes</a>
        </div>

        <div class="card">
            <div class="card-body">
                <form action="/quick-notes/<%= quickNote._id %>?_method=PUT" method="POST">
                    <div class="mb-3">
                        <label for="content" class="form-label">Note Content</label>
                        <textarea 
                            name="content" 
                            id="content"
                            class="form-control" 
                            rows="6" 
                            required
                        ><%= quickNote.content %></textarea>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">💾 Save Changes</button>
                        <a href="/quick-notes" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>