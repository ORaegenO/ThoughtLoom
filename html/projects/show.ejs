<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 style="border-left: 4px solid '<%= project.color || '#000' %>'; padding-left: 12px;"><%= project.name %></h1>
            <a href="/projects" class="btn btn-outline-secondary">Back to Projects</a>
        </div>
        
        <p class="text-muted"><%= project.description || 'No description' %></p>
        
        <hr>
        
        <h3>Categories</h3>
        <p class="text-muted">Categories coming soon! We'll add them in the next step.</p>
        
        <a href="/categories/new?project=<%= project._id %>" class="btn btn-primary">+ New Category</a>
    </div>
</body>
</html>